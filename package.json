{
  "private": true,
  "name": "erml-talk",
  "scripts": {
    "make": "vite --host --port 8080",
    "make:force": "yarn make --force",
    "build": "vite build",
    "show": "vite preview --host"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.3.11",
    "tailwindcss": "^2.2.19",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "reveal.js": "^4.2.0"
  }
}
